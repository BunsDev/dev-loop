# === Required for TB-1 ===
LINEAR_API_KEY=lin_api_xxxxx
ANTHROPIC_API_KEY=sk-ant-xxxxx
GITHUB_TOKEN=ghp_xxxxx

# === OpenObserve (local, defaults work) ===
OPENOBSERVE_URL=http://localhost:5080
OPENOBSERVE_USER=admin@dev-loop.local
OPENOBSERVE_PASS=devloop123

# === Required for TB-3 (Security Gate) ===
# AIKIDO_API_KEY=

# === Required for TB-4 (Code Review Gate) ===
# CODERABBIT_API_KEY=

# === Optional ===
# LINEAR_WEBHOOK_SECRET=         # For webhook mode (polling doesn't need this)
# OPENOBSERVE_ORG=default        # OpenObserve organization
# COST_CEILING_DEFAULT=2.00      # Default per-run budget in USD
# POLL_INTERVAL_SEC=60           # Linear polling interval
